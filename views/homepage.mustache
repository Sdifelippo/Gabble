<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8" name="viewport" content="width=device-width, initial-scale=1">
  <title>Welcome to Gabble</title>
  <link rel="stylesheet" href="styles.css">
  <link rel="shortcut icon" href="" type="">
</head>

<body>
  <div class="header">

    <h1>Welcome to Gabble</h1>
  </div>
    <div class="user_wrapper">
    <div class="user">

      <p id="greeting">Greets! {{user.username}}</p>
</div>
        <div class="sign_out">
          <form class="" action="/logout" method="post">
            <input type="submit" name="" value="Sign out!">
          </form>
      </p>
      </div>
    </div>
  </div>
  <div class="post_msg">
     <button><a href="/message/new">Create a post!</a></button>
  </div>

  {{#messages}}
    <div class="user_msg">
      <div class="msg">
        <h3><small>{{username}}</small>: {{title}}</h3>
        <blockquote>{{body}}</blockquote>
        <small>Created on {{createAt}}</small>

      <div class="like">
        <form class="" action="/message/{{_id}}/like" method="post">
          <button class="myButton" type="submit">✔</button>
          <div class="tally">
            <a href="/message/{{id}}/user">{{totalLikes}} Likes</a>
          </div>
        </form>
      </div>

</div>

      <form class="delete_msg" action="/message/{{_id}}/delete" method="post">
        <button class="deleteButton" type="submit" name="button">Delete</button>
      </form>
    </div>
  {{/messages}}
</body>

</html>
